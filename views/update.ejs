<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <h1 class="text-center text-danger">Update List</h1>
        <form action="/updateList/<%= id %>" method="post" class="d-flex align-items-center gap-4 flex-column p-3 rounded-3 shadow col-10 col-md-8 col-lg-5 mx-auto">
            <div class="w-100">
                <input name="name" value="<%= list.name %>" type="text" placeholder="Edit Product Name" class="form-control w-100">
            </div>
            <div class="w-100">
                <input name="price" value="<%= list.price %>" type="number" placeholder="Edit Price" class="form-control w-100">
            </div>
            <div class="w-100">
                <input name="quantity" value="<%= list.quantity %>" type="number" placeholder="Edit Quantity" class="form-control w-100">
            </div>
            <div class="w-100">
                <input name="subTotal" value="<%= list.subTotal %>" type="number" placeholder="Edit Sub Total" class="form-control w-100">
            </div>
            <button type="submit" class="btn btn-danger w-100">Update List</button>
        </form>
</body>
</html>